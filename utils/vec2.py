import pygame
import math

Vec2 = pygame.math.Vector2
# v.length()                
# v.length_squared()       
# v.normalize()             -> Scales vector to length 1 (In-place!)
# v.copy()                  
# v.distance_to(other)      
# v.angle_to(other)         -> Signed angle difference in degrees
# v.rotate_rad(rad)         -> Returns new vector rotated by radians
# v.dot(other)              
# v.cross(other)            
# v.lerp(target, t)         -> Linear interpolate toward target (0.0 to 1.0)
